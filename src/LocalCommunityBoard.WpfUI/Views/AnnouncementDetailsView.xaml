<UserControl x:Class="LocalCommunityBoard.WpfUI.Views.AnnouncementDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FFF8F8F8">
    <ScrollViewer>
        <StackPanel Margin="20" Width="600" HorizontalAlignment="Center">
            <!-- Announcement Details -->
            <Image Height="300" Stretch="UniformToFill" Source="{Binding Image}" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" TextAlignment="Center"/>
            <TextBlock Text="{Binding Body}" FontSize="16" TextWrapping="Wrap" TextAlignment="Left"/>

            <Separator Margin="0,20,0,20"/>

            <!-- Comments Section -->
            <TextBlock Text="Comments" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <ItemsControl x:Name="CommentsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#CCC" BorderThickness="1" CornerRadius="8" Margin="0,5,0,5" Padding="10">
                            <StackPanel>
                                <TextBlock Text="{Binding User.Username}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Body}" TextWrapping="Wrap" Margin="0,3,0,0"/>
                                <TextBlock Text="{Binding CreatedAt}" FontStyle="Italic" FontSize="11" Foreground="Gray"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Add Comment -->
            <TextBox x:Name="CommentBox" Height="60" TextWrapping="Wrap" Margin="0,15,0,5" />
            <Button Content="Post Comment"
                    Width="150"
                    HorizontalAlignment="Right"
                    Click="PostComment_Click"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
