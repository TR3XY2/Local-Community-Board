<UserControl x:Class="LocalCommunityBoard.WpfUI.Views.AnnouncementDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FFF8F8F8">
    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center" Width="600">
            <Image Height="300" Stretch="UniformToFill" Source="{Binding ImageUrl}" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" TextAlignment="Center"/>
            <TextBlock Text="{Binding Body}" FontSize="16" TextWrapping="Wrap" TextAlignment="Left"/>
        </StackPanel>
    </Grid>
</UserControl>
