<Window x:Class="LocalCommunityBoard.WpfUI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Email:" Margin="0,10,0,5"/>
        <TextBox x:Name="EmailBox" Grid.Row="1" Margin="0,0,0,10"/>

        <TextBlock Text="Password:" Grid.Row="2" Margin="0,10,0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3"/>

        <Button Content="Login" Grid.Row="4" Height="30" Width="100"
                HorizontalAlignment="Right" Margin="0,20,0,0"
                Click="Login_Click"/>
    </Grid>
</Window>